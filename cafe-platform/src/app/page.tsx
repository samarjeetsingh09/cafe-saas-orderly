export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-3 p-8">
      <h1 className="text-3xl font-bold text-primary">Cafe QR Ordering Platform</h1>
      <p className="text-muted">Scan, order, pay, eat — no waiter wait, no commission.</p>
    </main>
  );
}
